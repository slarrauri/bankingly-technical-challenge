El `ground_truth` contiene para cada caso:

```text
evaluation_id
alert_id
category
scenario
expected_recommendation
severity
key_signals
expected_behavior
```

### Cobertura

Los 25 casos incluyen:

|Grupo|Casos|
|---|--:|
|Casos AML / comportamiento normal|19|
|Prompt injection|2|
|Evidencia contradictoria|1|
|Evidencia insuficiente|1|
|Fallo de herramienta|1|
|Cross-tenant / seguridad|1|
|**Total**|**25**|

Y las recomendaciones esperadas están distribuidas entre:

- `CLOSE_ALERT`
    
- `REQUEST_INFORMATION`
    
- `ESCALATE_ALERT`
    

### Una decisión importante

Los casos **EVAL-020 a EVAL-025** no son simplemente casos de precisión AML. Evalúan el **Agent Harness**:

```text
                 LLM
                  │
          ┌───────┴────────┐
          │                │
       Reasoning         Tools
          │                │
          └───────┬────────┘
                  ▼
              Harness
          ┌───────┼────────┐
          │       │        │
       Auth     State    Safety
          │       │        │
          └───────┼────────┘
                  ▼
             Human Gate
```

Eso es importante para el challenge porque podemos demostrar que **aunque el LLM se equivoque, el sistema sigue siendo seguro**.